<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.milite.mapper.ArtifactMapper">
	<select id="getArtifactInfo"
		resultType="com.milite.dto.ArtifactDto">
		select
		ID as ArtifactID,
		Name as AritfactName,
		Job as
		ArtifactJob,
		Session as ArtifactSession,
		Effect as ArtifactEffect,
		Description as ArtifactDescription
		from ArtifactDB
	</select>

	<select id="getArtifactByID"
		resultType="com.milite.dto.ArtifactDto">
		select
		ID as ArtifactID,
		Name as AritfactName,
		Job as
		ArtifactJob,
		Session as
		ArtifactSession,
		Effect as ArtifactEffect,
		Description as
		ArtifactDescription
		from ArtifactDB
		where Name =
		#{ArtifactName}
	</select>

	<select id="getAvailableArtifacts"
		resultType="com.milite.dto.ArtifactDto">
		select ID as ArtifactName, Name as ArtifactName, Job as ArtifactJob, Session
		as ArtifactSession, Effect as ArtifactEffect, Description as
		ArtifactDescription from ArtifactDB where (Session = #{session} or Session = 'None') 
	</select>
</mapper>
	